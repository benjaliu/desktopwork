# Rust build output
shell/src-tauri/target/

# Bundled server (populated by GitHub Actions build, not tracked in git)
shell/src-tauri/server/


# Node modules
node_modules/
desktop-agent/node_modules/
shell/node_modules/

# Build outputs
desktop-agent/dist/

# Bundles — runtime assets, generated by extract script at build time
desktop-agent/vendor/bundles/

# OpenClaw runtime dependency (cloned at build time)
vendor/OpenClaw/

# Lock files (keep pnpm-lock.yaml in repo, ignore others)
package-lock.json
pnpm-debug.log
*.log

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Tauri
shell/src-tauri/src-tauri/target/

# Old project leftovers (root level only)
/src-tauri/
.vscode/