# Rust build output
**/target/
**/Cargo.lock

# Bundled server (populated by GitHub Actions build, not tracked in git)
shell/src-tauri/server/

# Node binary sidecars (downloaded by GitHub Actions workflow, not tracked in git)
shell/node-binaries/


# Node modules
node_modules/
desktop-agent/node_modules/
shell/node_modules/
src-tauri/node_modules/

# Build outputs
desktop-agent/dist/
src-tauri/App/dist/

# Markdown bundle (esbuild output, regenerated by `pnpm build:markdown` and CI)
desktop-agent/apps/_shared/markdown.bundle.js

# Lock files (keep pnpm-lock.yaml in repo, ignore others)
package-lock.json
pnpm-debug.log
*.log

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# VS Code / IDE configs (per-developer)
**/.vscode/

# Vim swap files
*.swp
*.swo
*.swn
